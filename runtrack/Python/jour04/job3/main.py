#job3 insertion dans liste
def ma_list():
    #on créé la variable et init
    liste = ["pomme","cerise","orange"]
    #on ajoute un élément
    liste.append(("melon"))
    #on imprime
    print(liste)
#on appelle la méthode
ma_list()