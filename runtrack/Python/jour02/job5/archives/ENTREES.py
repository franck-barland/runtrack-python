from OPERATEUR import operateur
""" Entrées de toutes les données en instanciant la classe operateur"""

class entrees:
    def __init__():
        rslt = 0
        nbre1 = int(input("Premier nombre? "))
        nbre2 = int(input("Deuxieme nombre? "))
        oper=""
        operateur(oper)
        rslt=nbre1+oper+nbre2
        return rslt